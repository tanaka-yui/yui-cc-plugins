=== MANDATORY EXECUTION SEQUENCE ===
You are running in superpowers mode with a STRICT execution sequence.
You MUST follow these phases IN ORDER. Skipping any phase is a critical error.

PHASE 1 — BRAINSTORMING (required, do this FIRST):
  Use the Skill tool to invoke "superpowers:brainstorming" immediately.
  Do NOT read any files, do NOT make any plans, do NOT write any code before
  completing brainstorming.

PHASE 2 — PLANNING (automatic transition from brainstorming):
  After brainstorming completes, you will be in writing-plans mode.
  Write a structured implementation plan.

PHASE 3 — EXECUTION:
  After the plan is approved, execute it.

VIOLATION: If you start writing code or making changes without completing
Phase 1 (brainstorming) and Phase 2 (planning), you are operating incorrectly.
Stop and use the Skill tool to invoke "superpowers:brainstorming".
=== END MANDATORY EXECUTION SEQUENCE ===

=== MANDATORY MODEL SELECTION SEQUENCE ===
You will operate in two phases. This sequence is REQUIRED even in auto mode.

OUTPUT LANGUAGE: all user-facing questions, option labels, tables, and progress
reports MUST be rendered in Japanese. The templates in this prompt are written in
English for consistency; translate them when presenting them to the user.

TEAM: dispatch-yui-cc-plugins

PHASE A — Planning / Brainstorming (resolved design role):
  Runner: claude; engine: claude; model: opus[1m]; effort: xhigh.
  Use that resolved runner/model for plan / brainstorming. Do NOT infer a model from
  the engine and do NOT switch models in this phase.
  - superpowers mode: invoke "superpowers:brainstorming" then write a plan
  Remember the path of the plan file you wrote — Phase B hands it off.

PHASE A-R — Plan/Spec review by the resolved review role (REQUIRED between Phase A and Phase B):
  Review policy/runner/engine/model/effort: fixed / claude / claude / opus[1m] / xhigh.
  It runs in a dedicated plain review session with NO status.json ownership; the review
  engine equals the design engine here, which is expected under a fixed policy.
  NEVER touch .assigned-setup-model-effort-review during review.

  Review points (run the round loop below at EACH point, in order):
    - superpowers mode: two points — id "spec" (after the brainstorming design doc),
      then id "plan" (after the implementation plan)

  Setup (before the first point only):
    mkdir -p "/Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/review"
    REVIEW_SURFACE=$(jq -r '.review.surface_id // empty' "/Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/prewarm.json")
    The SAME pane is reused across all points and rounds (it keeps review context).

  Round loop (at point <point>, N = 1..5):
    1. Compose the request text:
         "Review the <point> document at <ABSOLUTE_DOC_PATH>.
          Reference material: <related file paths>.
          This is round <N> (max 5)."
       For N >= 2 append:
         "Previous findings: /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/review/<point>-round-<N-1>.md.
          The document was revised in response — check whether concerns were
          addressed (rebuttals are inline below) and look for new issues.
          <your rebuttals to findings you rejected, with reasons>"
       Always append the protocol:
         "Write your findings as markdown to
          /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/review/<point>-round-<N>.md.
          The LAST line of that file MUST be exactly 'VERDICT: approve' or
          'VERDICT: needs_work'. approve = the document is ready to implement."
    1b. Append the parallel-review directive for the review pane engine:
          PARALLEL=$(bash /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/apps/cmux-team-dispatch-task/skills/cmux-team-dispatch-task/scripts/parallel-directive.sh --engine claude --mode review)
          <request text>="<request text> $PARALLEL"
    2. Send the request with ONE send-prompt.sh call, then wait by polling the
       verdict file:
         bash /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/apps/cmux-team-dispatch-task/skills/cmux-team-dispatch-task/scripts/send-prompt.sh --to-surface "$REVIEW_SURFACE" \
           --agmsg-team dispatch-yui-cc-plugins --agmsg-to setup-model-effort-review --agmsg-from setup-model-effort \
           --label review-plan --outbox-dir "/Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/outbox" \
           -- "<request text>"
       Then poll the verdict file (5s interval, in 15-min chunks with NO overall time
       limit while the reviewer pane is active — liveness is judged by a screen-snapshot
       diff at every chunk boundary, using
       `cmux read-screen --workspace "$CMUX_WORKSPACE_ID" --surface "$REVIEW_SURFACE"`).
       A verdict written during the final sleep must win over the liveness check.
       Two consecutive all-failed read-screen boundaries count as a vanished pane.
    3. Read the verdict:
       - "VERDICT: approve" → this point is done. Move to the next point; after the LAST
         point, proceed to Phase B. Leave the review pane OPEN and idle.
       - "VERDICT: needs_work" → read the findings. Apply the ones you judge valid;
         collect reasons for the ones you reject (rebuttals for the next round). Then
         N < 5 → round N+1; N == 5 → summarize unresolved findings and ask via
         AskUserQuestion whether to proceed as is or revise further.
       - Stalled → check the verdict file once more; if still nothing, re-send the SAME
         round's request once. If it stalls again, ask via AskUserQuestion whether to
         re-send or skip the review.

  VIOLATION: do NOT start Phase B before every review point reached approve or an
  explicit user decision was made.

PHASE B — Execution model default is fixed to "claude" (skip AskUserQuestion):
  After the plan is approved and Phase A-R passed, skip AskUserQuestion and immediately
  run the claude delegation branch below. Do NOT invent a new execution path.

  Resolved execution role: choice claude / runner claude / engine claude / model sonnet /
  effort high. The design role is opus[1m]/xhigh, so the roles differ and this task
  DELEGATES — it does not implement in this session.

  Delegation steps:
    1. Leave every other role pane recorded in prewarm.json OPEN and idle — do NOT close
       it. An unassigned standby holds no `.assigned-<name>` sentinel, so it never writes
       status.json.
    2. touch "/Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/.assigned-setup-model-effort-claude"
    3. Send the execution request with ONE send-prompt.sh call:
         EXEC_SURFACE=$(jq -r '.executors.claude.surface_id // empty' "/Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/prewarm.json")
         PARALLEL=$(bash /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/apps/cmux-team-dispatch-task/skills/cmux-team-dispatch-task/scripts/parallel-directive.sh --engine claude --mode execute)
         REVIEW_PARALLEL=$(bash /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/apps/cmux-team-dispatch-task/skills/cmux-team-dispatch-task/scripts/parallel-directive.sh --engine claude --mode review)
       REQUEST_TEXT must be the EXTENDED version (Phase B-R is enabled) described below.
         bash /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/apps/cmux-team-dispatch-task/skills/cmux-team-dispatch-task/scripts/send-prompt.sh --to-surface "$EXEC_SURFACE" \
           --agmsg-team dispatch-yui-cc-plugins --agmsg-to setup-model-effort-claude --agmsg-from setup-model-effort \
           --label phase-b-exec --outbox-dir "/Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/outbox" \
           -- "$REQUEST_TEXT"
    4. touch "/Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/.deferred", then follow PHASE B-R for your post-delegation role.

PHASE B-R — Post-implementation code review (REQUIRED before the PR / merge):
  Review point id: "code". Findings file: /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/review/code-round-<N>.md — the
  LAST line MUST be exactly 'VERDICT: approve' or 'VERDICT: needs_work'. Max 5 rounds.

  Reviewer assignment (fixed policy): REVIEWER_SURFACE = prewarm.json .review.surface_id.
  REVIEWER_RUNNER = claude, REVIEWER_ENGINE = claude, REVIEWER_WORKSPACE = $CMUX_WORKSPACE_ID.
  The dedicated review pane reviews every implementation choice, including an implementer
  with the same engine. After delegation YOU (the design pane) touch `.deferred` and exit;
  you never become the reviewer.

  Before sending REQUEST_TEXT, write the reviewer wiring file:
    mkdir -p "/Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/review"
    jq -n --arg s "$REVIEWER_SURFACE" --arg w "$CMUX_WORKSPACE_ID" \
      --arg d "/Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/review" --arg r "claude" --arg e "claude" \
      '{reviewer_surface: $s, reviewer_workspace: $w, review_dir: $d, reviewer_runner: $r, reviewer_engine: $e}' \
      > "/Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/review/code-review.json"

  Extended REQUEST_TEXT (fill every <...> before sending):
    "Read and execute the plan at <PLAN_FILE_PATH>. $PARALLEL After all changes are
     committed and BEFORE finishing, you MUST get a code review approval. Round N starts
     at 1, max 5 rounds. Each round:
     (1) send the review request with ONE command:
           bash /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/apps/cmux-team-dispatch-task/skills/cmux-team-dispatch-task/scripts/send-prompt.sh --to-surface <REVIEWER_SURFACE> \
             --agmsg-team dispatch-yui-cc-plugins --agmsg-to setup-model-effort-review --agmsg-from setup-model-effort-claude \
             --label review-code --outbox-dir /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/outbox \
             -- '<request text>'
         where <request text> is: code review round N: review the committed changes on
         this branch against the plan at <PLAN_FILE_PATH>; write findings to
         /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/review/code-round-N.md whose LAST line must be VERDICT: approve or
         VERDICT: needs_work. From round 2 append your rebuttals to the findings you
         rejected, with reasons. Also include this in the message to the reviewer,
         addressed to the reviewer and not to you: $REVIEW_PARALLEL End of the message to
         the reviewer.
     (2) wait by polling /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/review/code-round-N.md every 5 seconds for a VERDICT
         line, in 15-minute chunks with NO overall time limit while the reviewer is active.
         Judge liveness with
         `cmux read-screen --workspace $CMUX_WORKSPACE_ID --surface <REVIEWER_SURFACE>`
         snapshots at each chunk boundary; re-check the verdict file at every boundary
         before judging stalled.
     (3) On VERDICT: approve, finish per the instructions below. On VERDICT: needs_work,
         apply the findings you judge valid, commit, and start round N+1. If round 5 still
         ends with needs_work, ask via AskUserQuestion whether to proceed or run one more
         round. If the wait exits stalled, check the verdict file one last time, then
         re-send the same round once; if it stalls again, ask via AskUserQuestion.
     *** ABORT / ESCALATION PROTOCOL — this overrides everything above ***
     If at ANY point you decide to STOP without completing the work — a blocking error, a
     design contradiction, a decision that the plan is wrong, or simply giving up — you
     MUST NOT stop silently. Writing status.json is NOT a notification. Before you stop,
     do ALL of the following, in this order:
       1. Write the reason to /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/review/code-round-<current N>.md with the LAST
          line exactly 'VERDICT: needs_work'. If you never sent a review request, use N=1.
       2. Notify the REVIEWER:
            bash /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/apps/cmux-team-dispatch-task/skills/cmux-team-dispatch-task/scripts/send-prompt.sh --to-surface <REVIEWER_SURFACE> \
              --agmsg-team dispatch-yui-cc-plugins --agmsg-to setup-model-effort-review --agmsg-from setup-model-effort-claude \
              --label abort-reviewer --outbox-dir /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/outbox \
              -- '[abort] <one-line reason>'
       3. Write status.json with status "error" and the reason as message.
       4. Notify the PARENT with the SAME single send-prompt.sh call as (a) below, but
          with "status: error".
       5. Run /exit so the runner wrapper can finalize.
     *** END ABORT / ESCALATION PROTOCOL ***

     After all work is committed (this dispatch uses Wait-and-merge, so do NOT create a
     PR and do NOT push), do these two things IN THIS ORDER. Neither may be skipped.
     (a) MANDATORY completion notification. You received this request as typed text and
         never read the task prompt file, so no other status protocol applies to you —
         this command is the only thing that informs the parent:
           bash /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/apps/cmux-team-dispatch-task/skills/cmux-team-dispatch-task/scripts/send-prompt.sh --to-workspace 2D4ADB77-A086-4A3B-856A-9DF6A27F4AF6 \
             --agmsg-team dispatch-yui-cc-plugins --agmsg-to parent --agmsg-from setup-model-effort-claude \
             --label dispatch-notify --outbox-dir /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/outbox \
             -- '[dispatch] task setup-model-effort finished (status: done)'
     (b) Run /exit so the runner wrapper can finalize. Do NOT leave the session idle."

  VIOLATION: do NOT finish before the code review reached approve or one of the explicit
  fallbacks above was taken.

  Notes on the deferred completion mechanism:
    - This session was launched with `--defer-status`, so its runner wrapper checks for
      /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/.deferred at exit. When the file exists, the wrapper skips status.json
      update, parent notification, and signal emission — letting the executor's wrapper
      own those.

VIOLATION: Do NOT skip Phase B. Follow the exact flow defined in this PHASE B block.
PLAN-MODE TRAP: plan approval ("start implementing") does NOT override this sequence.
After the plan is approved, BEFORE editing any file, return to this block and complete
Phase A-R then Phase B.
=== END MANDATORY MODEL SELECTION SEQUENCE ===

=== YOUR TASK ===

Repository: yui-cc-plugins (a pnpm + turbo monorepo of Claude Code plugins for the cmux
terminal multiplexer). You are working in an isolated git worktree on branch
`feat/setup-model-effort`.

Target plugin: `apps/cmux-team-dispatch-task`.

## What to build

**`--setup` lets the user pick a runner, but not a model or an effort.** Extend it so
that model AND effort can be selected for **all three roles — design, review, exec**.

The plugin already stores per-role model and effort in
`~/.claude/cmux-team-dispatch-task/runners.json` as six optional per-runner fields:
`plan_model` / `review_model` / `exec_model` and `plan_effort` / `review_effort` /
`exec_effort`. As of v1.20.0 these are **engine-neutral** — they work for `claude`
runners as well as `codex` ones. What is missing is a way to set them through `--setup`
without hand-editing JSON.

## Where the current behaviour lives

- `skills/cmux-team-dispatch-task/references/setup-mode.md` — the runtime source of truth
  for `--setup` and `--reset` (English). Its Japanese mirror is `setup-mode-ja.md`.
- `skills/cmux-team-dispatch-task/SKILL.md` — the `## Setup Mode` delegation section and
  the **First-run setup** section (which already asks for the six fields when registering
  a NEW runner). Read that First-run field list: it is the closest existing precedent for
  the question shapes you need, including the engine-specific option lists.
- `skills/cmux-team-dispatch-task/scripts/config-edit.sh` — the ONLY writer for
  `config.json`. Note it handles the five **role keys** (`design_runner` / `review_runner`
  / `exec_choice` / `review_mode` / `prewarm`) and deliberately rejects unknown keys.
  Per-runner model/effort live in `runners.json`, which `config-edit.sh` does NOT touch.
  Decide during brainstorming whether the runners.json writes belong in a new script, an
  extension of this one, or inline in the skill — and justify the choice.

## Facts you must respect

- **Defaults when a field is unset** (do not change these):

  | role | claude model | codex model | effort (both engines) |
  |------|--------------|-------------|------------------------|
  | plan | `opus[1m]` | none (codex-side default) | `xhigh` |
  | review | `opus[1m]` | required when chosen as reviewer | `xhigh` |
  | exec | `sonnet` | none (codex-side default) | `high` |

- **Effort allowlist is engine-specific**: claude accepts `low|medium|high|xhigh|max`;
  codex accepts `minimal|low|medium|high|xhigh`. **Never offer `max` for a codex
  engine** — whether codex accepts it was never confirmed, so the safe branch stands.
- **Model strings are NOT validated.** Offer the common claude aliases
  (`opus[1m]` / `sonnet` / `fable`) as choices but let any string through.
- `AskUserQuestion` allows at most **4 questions per call and 4 options per question**.
  Six fields across three roles will not fit in one call — design the call sequence
  around that limit, and keep a "leave unchanged" option available for every field.
  The existing `--setup` flow already solves the ">4 runners" case by offering the first
  few plus the automatic "Other" free-text field; reuse that pattern rather than
  inventing a new one.
- `--setup` must keep writing through the existing atomic pattern (writer-specific
  `mktemp` + `jq` + `mv` only on jq success) so a concurrent writer cannot corrupt the
  file, and so keys owned by other components (`shell_ready_ms` in config.json) survive.
- `--setup` is mutually exclusive with `--loop`, `--reset` and `--override`, and
  dispatches nothing. Do not change that.

## Constraints

- `apps/cmux-team-dispatch-task/CLAUDE.md` has a **hard four-file documentation rule**:
  `SKILL.md`, `references/guide-ja.md`, `README.md`, and `CLAUDE.md` must agree and be
  updated in the same commit. `setup-mode.md` / `setup-mode-ja.md` must also stay
  mirrored.
- **`SKILL.md` and every `references/*.md` NOT named `*-ja.md` must contain ZERO Japanese
  characters.** Only `*-ja.md` files, `README.md` and `CLAUDE.md` are Japanese. Enforced
  by `node scripts/check-doc-lang.mjs` — a hard gate.
- Shell scripts must stay **bash 3.2 compatible** (macOS default). Under `set -u`, expand
  possibly-empty arrays with the `${arr[@]+"${arr[@]}"}` idiom.
- Comments and commit messages in Japanese; code identifiers in English.
- Do NOT bump any version number and do NOT push — the parent merges locally.
- **Every test that invokes `prewarm-panes.sh` must `mkdir -p` its worktree directory
  first.** Otherwise the script's `git worktree add` runs against the real repository and
  leaves branches and worktree registrations behind. This has bitten this repo twice.
- Extend `apps/cmux-team-dispatch-task/test/test-setup-skill.sh` (or add a sibling suite)
  with regression coverage. Make sure any negative assertion first checks that the file
  it greps actually exists.

## Verification gates

Run all of these from the worktree root before reporting done:

```
cd apps/cmux-team-dispatch-task
for t in test/*.sh; do printf '%-46s ' "$t"; if bash "$t" >/dev/null 2>&1; then echo OK; else echo FAILED; fi; done
cd /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins && pnpm check
```

Every suite must pass and `check-doc-lang` must print OK. The 4 pre-existing
`noNonNullAssertion` warnings in `@tanaka-yui/token-meter` are known noise, not failures.

After running the tests, also confirm the suite left no residue:
`git worktree list` and `git branch --list 'feat/pg*' 'feat/is*' 'feat/ov*'`.
=== END YOUR TASK ===

PROGRESS REPORTING FORMAT:
When reporting progress to the parent (or in your own visible output), you MUST
use the following box drawing table. Do NOT free-form the layout.
The header row, the column widths, and the fixed token values are NOT translatable:
keep the headers (# / Task / Surface / Mode / Status / Last message) and the mode /
status tokens listed below exactly as written. The OUTPUT LANGUAGE rule applies only
to the free-text cells (Task and Last message).

┌────┬──────────────────────────┬──────────┬────────────┬───────────┬─────────────────────────┐
│ #  │ Task                     │ Surface  │ Mode       │ Status    │ Last message            │
├────┼──────────────────────────┼──────────┼────────────┼───────────┼─────────────────────────┤
│ 1  │ <this-task-slug>         │ <surf>   │ superpwr   │ <status>  │ <one-line message>      │
└────┴──────────────────────────┴──────────┴────────────┴───────────┴─────────────────────────┘

- mode ∈ {superpwr, plan}
- status ∈ {launched, executing, done, error}
- Truncate long messages with center ellipsis "…"

IMPORTANT: Status reporting protocol.
When you finish planning and begin execution, run:
  echo '{"status":"executing","message":"<brief description>","timestamp":"'$(date -u +%Y-%m-%dT%H:%M:%SZ)'"}' > /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/status.json

When all work is complete:
1. Stage and commit ALL changes before reporting done:
  git add -A
  git commit -m "setup-model-effort: <concise summary of changes>"
  If there are multiple logical units of work, create separate commits for each.
  Do NOT skip this step — uncommitted changes will be lost when the worktree is cleaned up.
  This dispatch uses Wait-and-merge: do NOT push and do NOT create a PR.

2. Then report completion. Do NOT ask cleanup questions here — the parent asks once at the
   end of dispatch.
  echo '{"status":"done","message":"<summary of changes>","timestamp":"'$(date -u +%Y-%m-%dT%H:%M:%SZ)'"}' > /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/status.json
And write a result summary to /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/result.md with sections:
  # <Task Name>
  ## Changes Made
  ## Test Results
  ## Commits

If you encounter a blocking error, run:
  echo '{"status":"error","message":"<error description>","timestamp":"'$(date -u +%Y-%m-%dT%H:%M:%SZ)'"}' > /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/status.json
Writing this file is NOT a notification — it is polled by the parent only. The
completion notification below is MANDATORY on the error path exactly as much as
on the done path. Send it immediately after writing status.json, then end this session.

You can message the parent directly at any time (questions, progress):
  ~/.agents/skills/agmsg/scripts/send.sh dispatch-yui-cc-plugins setup-model-effort parent "<message>"

MANDATORY completion notification: immediately after writing done/error to
status.json, notify the parent yourself with ONE send-prompt.sh call:
  bash /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/apps/cmux-team-dispatch-task/skills/cmux-team-dispatch-task/scripts/send-prompt.sh --to-workspace 2D4ADB77-A086-4A3B-856A-9DF6A27F4AF6 \
    --agmsg-team dispatch-yui-cc-plugins --agmsg-to parent --agmsg-from setup-model-effort \
    --label dispatch-notify --outbox-dir /Users/yui/Documents/workspace/tanaka-yui/yui-cc-plugins/.dispatch/setup-model-effort/outbox \
    -- "[dispatch] task \"setup-model-effort\" finished (status: <done|error>)"
send-prompt.sh types the message into the parent pane (the only thing that wakes an
idle parent) and, when the parent's agmsg watcher is alive, also records the same text
in its inbox — that record is a log, not a wake mechanism. Do NOT rely on session exit
either: the runner wrapper notifies on exit as a backstop, but an idle TUI session never
exits, and when agmsg is installed no monitor loop is running, so without this call the
parent may never be informed.

NOTE ON WHO SENDS THE FINAL NOTIFICATION: because this task DELEGATES implementation to
the claude executor pane (Phase B), the executor is the one that sends the final
"finished" notification and owns the status.json terminal transition. Your own
obligation is the ABORT path: if YOU stop before delegating, you must send the error
notification yourself.
