# codex-bridge Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Claude Code の `.claude/rules/*.md`（frontmatter マッピング）と `CLAUDE.md` を、Codex が読める `AGENTS.md` にネスト生成する汎用プラグイン `codex-bridge` を追加する。

**Architecture:** `apps/codex-bridge/` に標準プラグインを新設。`src/generate.ts` を「純粋関数（frontmatter パース / 結合 / グループ化 / 書き込み判定 / サイズ判定）」と「fs を触る収集・書き込み・main」に分離し、純粋関数を TDD で固める。スラッシュコマンド `/codex-bridge` は `bin/codex-bridge.ts`（Bun）を呼ぶ薄いラッパ。

**Tech Stack:** Bun ランタイム / TypeScript 6 / Bun test / biome。外部依存なし（frontmatter は最小自前パーサ）。

## Global Constraints

- **言語規約**: ドキュメント・コメント・コミットメッセージは日本語。コード（変数・関数・CLI フラグ）は英語。
- **TypeScript**: `any` / `unknown` 型を使わない。`class` は原則使わない。
- **biome 2.4.9**: `lineWidth: 120`, `indentStyle: space`, `indentWidth: 2`, single quote, `semicolons: asNeeded`。lint の `noUnusedImports` / `noUnusedVariables` は error。
- **ランタイム**: Bun（`bun run` / `bun test`）。tsconfig は cmux-team と同一設定（`noUncheckedIndexedAccess: true` に注意 = 配列/正規表現グループは `string | undefined`）。
- **バージョン同期**: `apps/codex-bridge/.claude-plugin/plugin.json` の version を更新したら `.claude-plugin/marketplace.json` にも同期。
- **生成物センチネル**: 生成ファイル先頭に `GENERATED by codex-bridge` を含むコメントを付与。センチネル無しの既存 `AGENTS.md` は手書き扱いで上書きしない。
- **サイズ上限**: Codex `project_doc_max_bytes` 既定 = 32768 バイト。超過は警告のみ（書き込みは行う）。

---

### Task 1: プラグイン雛形の作成

プラグイン manifest / package / tsconfig / marketplace エントリ / src スタブを作り、`check` が通る土台を用意する。

**Files:**
- Create: `apps/codex-bridge/.claude-plugin/plugin.json`
- Create: `apps/codex-bridge/package.json`
- Create: `apps/codex-bridge/tsconfig.json`
- Create: `apps/codex-bridge/src/generate.ts`（スタブ: 定数のみ）
- Modify: `.claude-plugin/marketplace.json`（`plugins` 配列末尾にエントリ追加）

**Interfaces:**
- Produces: `SENTINEL: string`, `SIZE_LIMIT: number`（後続タスクが import）

- [ ] **Step 1: plugin.json を作成**

`apps/codex-bridge/.claude-plugin/plugin.json`:

```json
{
  "name": "codex-bridge",
  "version": "0.1.0",
  "description": "Claude Code の rules / CLAUDE.md を Codex が読める AGENTS.md にネスト生成する汎用プラグイン。",
  "author": {
    "name": "tanaka-yui",
    "url": "https://github.com/tanaka-yui"
  },
  "repository": "https://github.com/tanaka-yui/yui-cc-plugins/apps/codex-bridge",
  "license": "MIT",
  "keywords": ["claude-code", "codex", "agents-md", "rules", "bridge"],
  "commands": "./commands/"
}
```

- [ ] **Step 2: package.json を作成**

`apps/codex-bridge/package.json`:

```json
{
  "name": "@tanaka-yui/codex-bridge",
  "version": "0.1.0",
  "description": "Claude Code の rules / CLAUDE.md を Codex が読める AGENTS.md にネスト生成する。",
  "type": "module",
  "bin": {
    "codex-bridge": "bin/codex-bridge.ts"
  },
  "scripts": {
    "check": "tsc --noEmit && biome check ./bin ./src",
    "check:fix": "biome check --write ./bin ./src",
    "format": "biome format ./bin ./src",
    "format:fix": "biome format --write ./bin ./src",
    "lint": "biome lint ./bin ./src",
    "lint:fix": "biome lint --write ./bin ./src"
  }
}
```

- [ ] **Step 3: tsconfig.json を作成（cmux-team と同一）**

`apps/codex-bridge/tsconfig.json`:

```json
{
  "compilerOptions": {
    "lib": ["ESNext"],
    "target": "ESNext",
    "module": "Preserve",
    "moduleDetection": "force",
    "jsx": "react-jsx",
    "allowJs": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "noEmit": true,
    "strict": true,
    "skipLibCheck": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noPropertyAccessFromIndexSignature": false,
    "types": ["bun"]
  },
  "include": ["bin/**/*.ts", "src/**/*.ts"]
}
```

- [ ] **Step 4: src/generate.ts スタブを作成**

`apps/codex-bridge/src/generate.ts`:

```ts
export const SENTINEL = 'GENERATED by codex-bridge'
export const SIZE_LIMIT = 32768
```

- [ ] **Step 5: marketplace.json にエントリ追加**

`.claude-plugin/marketplace.json` の `plugins` 配列末尾（`token-meter` の後）に追加:

```json
    {
      "name": "codex-bridge",
      "description": "Claude Code の rules / CLAUDE.md を Codex が読める AGENTS.md にネスト生成する汎用プラグイン。",
      "source": "./apps/codex-bridge",
      "version": "0.1.0",
      "license": "MIT",
      "category": "development",
      "tags": ["claude-code", "codex", "agents-md", "rules", "bridge"]
    }
```

（直前のエントリ末尾に `,` を追加するのを忘れない）

- [ ] **Step 6: check が通ることを確認**

Run: `cd apps/codex-bridge && bun install && pnpm check`
Expected: PASS（型エラー・lint エラーなし）

- [ ] **Step 7: commit**

```bash
git add apps/codex-bridge/.claude-plugin/plugin.json apps/codex-bridge/package.json apps/codex-bridge/tsconfig.json apps/codex-bridge/src/generate.ts .claude-plugin/marketplace.json
git commit -m "feat(codex-bridge): プラグイン雛形を追加"
```

---

### Task 2: frontmatter パーサ

rule ファイルの YAML frontmatter から `codexTargets` / `description` を取り出し、本文と分離する。

**Files:**
- Modify: `apps/codex-bridge/src/generate.ts`
- Test: `apps/codex-bridge/src/generate.test.ts`

**Interfaces:**
- Produces:
  - `interface RuleAttrs { codexTargets: string[]; description: string | null }`
  - `parseFrontmatter(content: string): { attrs: RuleAttrs; body: string }`

- [ ] **Step 1: 失敗するテストを書く**

`apps/codex-bridge/src/generate.test.ts`:

```ts
import { expect, test } from 'bun:test'
import { parseFrontmatter } from './generate.ts'

test('block 形式の codexTargets を配列で取り出す', () => {
  const input = ['---', 'description: Go backend rules', 'codexTargets:', '  - go/', '  - cmd/', '---', '# Go', 'body line'].join('\n')
  const { attrs, body } = parseFrontmatter(input)
  expect(attrs.codexTargets).toEqual(['go/', 'cmd/'])
  expect(attrs.description).toBe('Go backend rules')
  expect(body).toBe('# Go\nbody line')
})

test('flow 形式 [a, b] の codexTargets を取り出す', () => {
  const input = ['---', 'codexTargets: [go/, proto/]', '---', 'X'].join('\n')
  const { attrs } = parseFrontmatter(input)
  expect(attrs.codexTargets).toEqual(['go/', 'proto/'])
})

test('frontmatter が無ければ空 targets と全文 body を返す', () => {
  const { attrs, body } = parseFrontmatter('# no front\ntext')
  expect(attrs.codexTargets).toEqual([])
  expect(attrs.description).toBeNull()
  expect(body).toBe('# no front\ntext')
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd apps/codex-bridge && bun test src/generate.test.ts`
Expected: FAIL（`parseFrontmatter` が未 export）

- [ ] **Step 3: 実装を追加**

`apps/codex-bridge/src/generate.ts` に追記:

```ts
export interface RuleAttrs {
  codexTargets: string[]
  description: string | null
}

function stripQuotes(value: string): string {
  return value.replace(/^['"]|['"]$/g, '').trim()
}

function parseAttrs(raw: string): RuleAttrs {
  const targets: string[] = []
  let description: string | null = null
  let inTargets = false
  for (const line of raw.split(/\r?\n/)) {
    const listMatch = line.match(/^\s*-\s+(.+?)\s*$/)
    if (inTargets && listMatch) {
      targets.push(stripQuotes(listMatch[1] ?? ''))
      continue
    }
    inTargets = false
    const kv = line.match(/^([A-Za-z0-9_]+):\s*(.*)$/)
    if (!kv) continue
    const key = kv[1] ?? ''
    const value = (kv[2] ?? '').trim()
    if (key === 'codexTargets') {
      if (value.startsWith('[')) {
        const inner = value.slice(1, value.endsWith(']') ? -1 : value.length)
        for (const part of inner.split(',')) {
          const target = stripQuotes(part.trim())
          if (target) targets.push(target)
        }
      } else if (value === '') {
        inTargets = true
      }
    } else if (key === 'description') {
      description = value === '' ? null : stripQuotes(value)
    }
  }
  return { codexTargets: targets, description }
}

export function parseFrontmatter(content: string): { attrs: RuleAttrs; body: string } {
  const empty: RuleAttrs = { codexTargets: [], description: null }
  if (!content.startsWith('---\n') && !content.startsWith('---\r\n')) {
    return { attrs: empty, body: content }
  }
  const end = content.indexOf('\n---', 4)
  if (end === -1) return { attrs: empty, body: content }
  const raw = content.slice(4, end)
  const body = content.slice(end + 4).replace(/^\r?\n/, '')
  return { attrs: parseAttrs(raw), body }
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd apps/codex-bridge && bun test src/generate.test.ts`
Expected: PASS（3 tests）

- [ ] **Step 5: commit**

```bash
git add apps/codex-bridge/src/generate.ts apps/codex-bridge/src/generate.test.ts
git commit -m "feat(codex-bridge): frontmatter パーサを追加"
```

---

### Task 3: AGENTS.md レンダリングとセンチネル判定

rule 本文群を生成ヘッダー付き AGENTS.md 文字列に結合し、既存ファイルがセンチネルを持つか判定する。

**Files:**
- Modify: `apps/codex-bridge/src/generate.ts`
- Test: `apps/codex-bridge/src/generate.test.ts`

**Interfaces:**
- Consumes: `SENTINEL`
- Produces:
  - `renderAgentsFile(sources: string[], bodies: string[]): string`
  - `hasSentinel(content: string): boolean`

- [ ] **Step 1: 失敗するテストを書く**

`generate.test.ts` に追記:

```ts
import { hasSentinel, renderAgentsFile, SENTINEL } from './generate.ts'

test('renderAgentsFile はセンチネルヘッダーと結合本文を出力する', () => {
  const out = renderAgentsFile(['.claude/rules/go-backend.md', '.claude/rules/go-testing.md'], ['# A\naaa', '# B\nbbb'])
  expect(out).toContain(SENTINEL)
  expect(out).toContain('.claude/rules/go-backend.md, .claude/rules/go-testing.md')
  expect(out).toContain('# A\naaa\n\n# B\nbbb')
  expect(out.endsWith('\n')).toBe(true)
})

test('hasSentinel は生成物を true、手書きを false と判定する', () => {
  const generated = renderAgentsFile(['x'], ['y'])
  expect(hasSentinel(generated)).toBe(true)
  expect(hasSentinel('# 手書きの AGENTS.md\n')).toBe(false)
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd apps/codex-bridge && bun test src/generate.test.ts`
Expected: FAIL（`renderAgentsFile` / `hasSentinel` 未 export）

- [ ] **Step 3: 実装を追加**

`generate.ts` に追記:

```ts
export function renderAgentsFile(sources: string[], bodies: string[]): string {
  const header = `<!-- ${SENTINEL}. DO NOT EDIT.\n     Source: ${sources.join(', ')} -->`
  const merged = bodies.map((b) => b.trim()).join('\n\n')
  return `${header}\n\n${merged}\n`
}

export function hasSentinel(content: string): boolean {
  return content.includes(SENTINEL)
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd apps/codex-bridge && bun test src/generate.test.ts`
Expected: PASS（5 tests）

- [ ] **Step 5: commit**

```bash
git add apps/codex-bridge/src/generate.ts apps/codex-bridge/src/generate.test.ts
git commit -m "feat(codex-bridge): AGENTS.md レンダリングとセンチネル判定を追加"
```

---

### Task 4: target ごとのグループ化

rule 群を `codexTargets` でグループ化し、target 未指定 rule を分離する。順序は決定的にする。

**Files:**
- Modify: `apps/codex-bridge/src/generate.ts`
- Test: `apps/codex-bridge/src/generate.test.ts`

**Interfaces:**
- Produces:
  - `interface RuleInput { file: string; targets: string[]; body: string }`
  - `interface TargetGroup { target: string; members: { file: string; body: string }[] }`
  - `groupRulesByTarget(rules: RuleInput[]): { groups: TargetGroup[]; unmapped: string[] }`

- [ ] **Step 1: 失敗するテストを書く**

`generate.test.ts` に追記:

```ts
import { groupRulesByTarget } from './generate.ts'

test('同一 target の rule を結合し、target・file 順にソートする', () => {
  const { groups, unmapped } = groupRulesByTarget([
    { file: 'go-testing.md', targets: ['go/'], body: 'T' },
    { file: 'go-backend.md', targets: ['go/'], body: 'B' },
    { file: 'proto.md', targets: ['proto/'], body: 'P' },
  ])
  expect(groups.map((g) => g.target)).toEqual(['go/', 'proto/'])
  expect(groups[0]?.members.map((m) => m.file)).toEqual(['go-backend.md', 'go-testing.md'])
  expect(unmapped).toEqual([])
})

test('複数 target を持つ rule は各 target に現れる', () => {
  const { groups } = groupRulesByTarget([{ file: 'shared.md', targets: ['a/', 'b/'], body: 'S' }])
  expect(groups.map((g) => g.target)).toEqual(['a/', 'b/'])
})

test('targets 空の rule は unmapped に入る', () => {
  const { groups, unmapped } = groupRulesByTarget([{ file: 'orphan.md', targets: [], body: 'O' }])
  expect(groups).toEqual([])
  expect(unmapped).toEqual(['orphan.md'])
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd apps/codex-bridge && bun test src/generate.test.ts`
Expected: FAIL（`groupRulesByTarget` 未 export）

- [ ] **Step 3: 実装を追加**

`generate.ts` に追記:

```ts
export interface RuleInput {
  file: string
  targets: string[]
  body: string
}

export interface TargetGroup {
  target: string
  members: { file: string; body: string }[]
}

export function groupRulesByTarget(rules: RuleInput[]): { groups: TargetGroup[]; unmapped: string[] } {
  const byTarget = new Map<string, { file: string; body: string }[]>()
  const unmapped: string[] = []
  for (const rule of rules) {
    if (rule.targets.length === 0) {
      unmapped.push(rule.file)
      continue
    }
    for (const target of rule.targets) {
      const members = byTarget.get(target) ?? []
      members.push({ file: rule.file, body: rule.body })
      byTarget.set(target, members)
    }
  }
  const groups: TargetGroup[] = [...byTarget.keys()].sort().map((target) => ({
    target,
    members: (byTarget.get(target) ?? []).sort((a, b) => a.file.localeCompare(b.file)),
  }))
  return { groups, unmapped: unmapped.sort() }
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd apps/codex-bridge && bun test src/generate.test.ts`
Expected: PASS（8 tests）

- [ ] **Step 5: commit**

```bash
git add apps/codex-bridge/src/generate.ts apps/codex-bridge/src/generate.test.ts
git commit -m "feat(codex-bridge): target ごとのグループ化を追加"
```

---

### Task 5: 書き込み判定とサイズ判定

既存ファイルの上書き可否（手書き保護）と、生成物のサイズ上限チェックを純粋関数で行う。

**Files:**
- Modify: `apps/codex-bridge/src/generate.ts`
- Test: `apps/codex-bridge/src/generate.test.ts`

**Interfaces:**
- Consumes: `hasSentinel`, `SIZE_LIMIT`
- Produces:
  - `type WriteDecision = { action: 'write' } | { action: 'skip-handwritten' }`
  - `decideWrite(existing: string | null, next: string): WriteDecision`
  - `checkSize(content: string, limit?: number): { ok: boolean; bytes: number }`

- [ ] **Step 1: 失敗するテストを書く**

`generate.test.ts` に追記:

```ts
import { checkSize, decideWrite } from './generate.ts'

test('存在しない/センチネル付きなら write、手書きなら skip-handwritten', () => {
  const generated = renderAgentsFile(['x'], ['y'])
  expect(decideWrite(null, generated).action).toBe('write')
  expect(decideWrite(generated, generated).action).toBe('write')
  expect(decideWrite('# 手書き\n', generated).action).toBe('skip-handwritten')
})

test('checkSize は 32768 バイト超過で ok=false', () => {
  expect(checkSize('a'.repeat(100)).ok).toBe(true)
  const big = checkSize('a'.repeat(40000))
  expect(big.ok).toBe(false)
  expect(big.bytes).toBe(40000)
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd apps/codex-bridge && bun test src/generate.test.ts`
Expected: FAIL（`decideWrite` / `checkSize` 未 export）

- [ ] **Step 3: 実装を追加**

`generate.ts` に追記:

```ts
export type WriteDecision = { action: 'write' } | { action: 'skip-handwritten' }

export function decideWrite(existing: string | null, _next: string): WriteDecision {
  if (existing === null) return { action: 'write' }
  if (hasSentinel(existing)) return { action: 'write' }
  return { action: 'skip-handwritten' }
}

export function checkSize(content: string, limit: number = SIZE_LIMIT): { ok: boolean; bytes: number } {
  const bytes = Buffer.byteLength(content, 'utf8')
  return { ok: bytes <= limit, bytes }
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd apps/codex-bridge && bun test src/generate.test.ts`
Expected: PASS（10 tests）

- [ ] **Step 5: commit**

```bash
git add apps/codex-bridge/src/generate.ts apps/codex-bridge/src/generate.test.ts
git commit -m "feat(codex-bridge): 書き込み判定とサイズ判定を追加"
```

---

### Task 6: プロジェクト/グローバル出力の収集（fs）

ディレクトリを走査して、生成すべき AGENTS.md の一覧（パス・内容・source）と警告を組み立てる。実ファイルは書かない。

**Files:**
- Modify: `apps/codex-bridge/src/generate.ts`
- Test: `apps/codex-bridge/src/generate.test.ts`

**Interfaces:**
- Consumes: `parseFrontmatter`, `groupRulesByTarget`, `renderAgentsFile`
- Produces:
  - `interface PlannedOutput { path: string; content: string }`
  - `interface CollectResult { outputs: PlannedOutput[]; warnings: string[] }`
  - `collectProjectOutputs(root: string): Promise<CollectResult>`
  - `collectGlobalOutput(home: string): Promise<CollectResult>`

**実装メモ:** target ディレクトリが存在しない場合は警告してスキップ（自動作成しない）。`.claude/rules` や `CLAUDE.md` が無い場合も警告。パスは `node:path` の `join`、存在確認は `node:fs/promises` の `stat`（無ければ null 扱い）。

- [ ] **Step 1: 失敗するテストを書く**

`generate.test.ts` に追記:

```ts
import { mkdir, mkdtemp, writeFile } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { collectGlobalOutput, collectProjectOutputs } from './generate.ts'

async function makeTmp(): Promise<string> {
  return await mkdtemp(join(tmpdir(), 'codex-bridge-'))
}

test('collectProjectOutputs は rules と root CLAUDE.md を出力に変換する', async () => {
  const root = await makeTmp()
  await mkdir(join(root, '.claude/rules'), { recursive: true })
  await mkdir(join(root, 'go'), { recursive: true })
  await writeFile(join(root, '.claude/rules/go-backend.md'), '---\ncodexTargets:\n  - go/\n---\n# Go\nbody')
  await writeFile(join(root, '.claude/rules/orphan.md'), '# no front')
  await writeFile(join(root, 'CLAUDE.md'), '# Project overview')

  const { outputs, warnings } = await collectProjectOutputs(root)
  const paths = outputs.map((o) => o.path)
  expect(paths).toContain(join(root, 'go', 'AGENTS.md'))
  expect(paths).toContain(join(root, 'AGENTS.md'))
  const goOut = outputs.find((o) => o.path === join(root, 'go', 'AGENTS.md'))
  expect(goOut?.content).toContain('# Go\nbody')
  expect(warnings.some((w) => w.includes('orphan.md'))).toBe(true)
})

test('collectProjectOutputs は存在しない target を警告してスキップする', async () => {
  const root = await makeTmp()
  await mkdir(join(root, '.claude/rules'), { recursive: true })
  await writeFile(join(root, '.claude/rules/x.md'), '---\ncodexTargets:\n  - missing/\n---\nX')
  const { outputs, warnings } = await collectProjectOutputs(root)
  expect(outputs.some((o) => o.path.includes('missing'))).toBe(false)
  expect(warnings.some((w) => w.includes('missing/'))).toBe(true)
})

test('collectGlobalOutput は ~/.claude/CLAUDE.md を ~/.codex/AGENTS.md に変換する', async () => {
  const home = await makeTmp()
  await mkdir(join(home, '.claude'), { recursive: true })
  await writeFile(join(home, '.claude/CLAUDE.md'), '必ず日本語で応答')
  const { outputs } = await collectGlobalOutput(home)
  expect(outputs[0]?.path).toBe(join(home, '.codex', 'AGENTS.md'))
  expect(outputs[0]?.content).toContain('必ず日本語で応答')
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd apps/codex-bridge && bun test src/generate.test.ts`
Expected: FAIL（`collectProjectOutputs` / `collectGlobalOutput` 未 export）

- [ ] **Step 3: 実装を追加**

`generate.ts` の先頭付近に import を追加:

```ts
import { readdir, readFile, stat } from 'node:fs/promises'
import { join } from 'node:path'
```

`generate.ts` 末尾に追記:

```ts
export interface PlannedOutput {
  path: string
  content: string
}

export interface CollectResult {
  outputs: PlannedOutput[]
  warnings: string[]
}

async function pathExists(target: string): Promise<boolean> {
  try {
    await stat(target)
    return true
  } catch {
    return false
  }
}

export async function collectProjectOutputs(root: string): Promise<CollectResult> {
  const outputs: PlannedOutput[] = []
  const warnings: string[] = []

  const rulesDir = join(root, '.claude', 'rules')
  if (await pathExists(rulesDir)) {
    const entries = (await readdir(rulesDir)).filter((f) => f.endsWith('.md')).sort()
    const rules: RuleInput[] = []
    for (const file of entries) {
      const raw = await readFile(join(rulesDir, file), 'utf8')
      const { attrs, body } = parseFrontmatter(raw)
      rules.push({ file, targets: attrs.codexTargets, body })
    }
    const { groups, unmapped } = groupRulesByTarget(rules)
    for (const orphan of unmapped) {
      warnings.push(`未マッピング rule (codexTargets 無し): .claude/rules/${orphan}`)
    }
    for (const group of groups) {
      const targetDir = join(root, group.target)
      if (!(await pathExists(targetDir))) {
        warnings.push(`target ディレクトリが存在しないためスキップ: ${group.target}`)
        continue
      }
      const sources = group.members.map((m) => `.claude/rules/${m.file}`)
      const bodies = group.members.map((m) => m.body)
      outputs.push({ path: join(targetDir, 'AGENTS.md'), content: renderAgentsFile(sources, bodies) })
    }
  } else {
    warnings.push('.claude/rules ディレクトリが見つかりません')
  }

  const claudeMd = join(root, 'CLAUDE.md')
  if (await pathExists(claudeMd)) {
    const content = await readFile(claudeMd, 'utf8')
    outputs.push({ path: join(root, 'AGENTS.md'), content: renderAgentsFile(['CLAUDE.md'], [content]) })
  } else {
    warnings.push('root CLAUDE.md が見つかりません')
  }

  return { outputs, warnings }
}

export async function collectGlobalOutput(home: string): Promise<CollectResult> {
  const source = join(home, '.claude', 'CLAUDE.md')
  if (!(await pathExists(source))) {
    return { outputs: [], warnings: ['~/.claude/CLAUDE.md が見つかりません'] }
  }
  const content = await readFile(source, 'utf8')
  return {
    outputs: [{ path: join(home, '.codex', 'AGENTS.md'), content: renderAgentsFile(['~/.claude/CLAUDE.md'], [content]) }],
    warnings: [],
  }
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd apps/codex-bridge && bun test src/generate.test.ts`
Expected: PASS（13 tests）

- [ ] **Step 5: commit**

```bash
git add apps/codex-bridge/src/generate.ts apps/codex-bridge/src/generate.test.ts
git commit -m "feat(codex-bridge): プロジェクト/グローバル出力の収集を追加"
```

---

### Task 7: 書き込み・main・CLI ラッパ

収集した出力を手書き保護・サイズ警告付きで書き込み、`main` で `--global` を分岐し、サマリを表示。`bin` から呼べるようにする。

**Files:**
- Modify: `apps/codex-bridge/src/generate.ts`
- Create: `apps/codex-bridge/bin/codex-bridge.ts`
- Test: `apps/codex-bridge/src/generate.test.ts`

**Interfaces:**
- Consumes: `collectProjectOutputs`, `collectGlobalOutput`, `decideWrite`, `checkSize`
- Produces:
  - `interface WriteReport { written: string[]; skippedHandwritten: string[]; oversize: { path: string; bytes: number }[] }`
  - `writeOutputs(outputs: PlannedOutput[]): Promise<WriteReport>`
  - `main(argv: string[], env: { cwd: string; home: string }): Promise<number>`

- [ ] **Step 1: 失敗するテストを書く**

`generate.test.ts` に追記:

```ts
import { readFile as readFileAssert } from 'node:fs/promises'
import { writeOutputs } from './generate.ts'

test('writeOutputs は新規書き込みし、手書きファイルはスキップする', async () => {
  const dir = await makeTmp()
  const generated = renderAgentsFile(['x'], ['y'])
  const newPath = join(dir, 'a', 'AGENTS.md')
  const handwrittenPath = join(dir, 'AGENTS.md')
  await writeFile(handwrittenPath, '# 手書き\n')

  const report = await writeOutputs([
    { path: newPath, content: generated },
    { path: handwrittenPath, content: generated },
  ])

  expect(report.written).toContain(newPath)
  expect(report.skippedHandwritten).toContain(handwrittenPath)
  expect(await readFileAssert(newPath, 'utf8')).toBe(generated)
  expect(await readFileAssert(handwrittenPath, 'utf8')).toBe('# 手書き\n')
})

test('writeOutputs は 32KB 超過を oversize に記録する（書き込みは行う）', async () => {
  const dir = await makeTmp()
  const path = join(dir, 'AGENTS.md')
  const content = renderAgentsFile(['x'], ['a'.repeat(40000)])
  const report = await writeOutputs([{ path, content }])
  expect(report.oversize.some((o) => o.path === path)).toBe(true)
  expect(await readFileAssert(path, 'utf8')).toBe(content)
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd apps/codex-bridge && bun test src/generate.test.ts`
Expected: FAIL（`writeOutputs` 未 export）

- [ ] **Step 3: writeOutputs / main を実装**

`generate.ts` の import に `mkdir`, `writeFile` を追加:

```ts
import { mkdir, readdir, readFile, stat, writeFile } from 'node:fs/promises'
import { dirname, join } from 'node:path'
```

`generate.ts` 末尾に追記:

```ts
export interface WriteReport {
  written: string[]
  skippedHandwritten: string[]
  oversize: { path: string; bytes: number }[]
}

export async function writeOutputs(outputs: PlannedOutput[]): Promise<WriteReport> {
  const report: WriteReport = { written: [], skippedHandwritten: [], oversize: [] }
  for (const output of outputs) {
    const existing = (await pathExists(output.path)) ? await readFile(output.path, 'utf8') : null
    if (decideWrite(existing, output.content).action === 'skip-handwritten') {
      report.skippedHandwritten.push(output.path)
      continue
    }
    await mkdir(dirname(output.path), { recursive: true })
    await writeFile(output.path, output.content)
    report.written.push(output.path)
    const size = checkSize(output.content)
    if (!size.ok) report.oversize.push({ path: output.path, bytes: size.bytes })
  }
  return report
}

export async function main(argv: string[], env: { cwd: string; home: string }): Promise<number> {
  const isGlobal = argv.includes('--global')
  const { outputs, warnings } = isGlobal
    ? await collectGlobalOutput(env.home)
    : await collectProjectOutputs(env.cwd)
  const report = await writeOutputs(outputs)

  console.log(`codex-bridge: ${isGlobal ? 'global' : 'project'} モードで実行`)
  for (const path of report.written) console.log(`  生成: ${path}`)
  for (const path of report.skippedHandwritten) console.log(`  スキップ(手書き): ${path}`)
  for (const item of report.oversize) console.log(`  警告(32KB 超過, 切り捨てリスク): ${item.path} = ${item.bytes} bytes`)
  for (const warning of warnings) console.log(`  警告: ${warning}`)
  console.log(`完了: ${report.written.length} 件生成 / ${report.skippedHandwritten.length} 件スキップ / ${warnings.length} 件警告`)
  return 0
}
```

- [ ] **Step 4: bin ラッパを作成**

`apps/codex-bridge/bin/codex-bridge.ts`:

```ts
#!/usr/bin/env bun

import { main } from '../src/generate.ts'

const code = await main(process.argv.slice(2), {
  cwd: process.cwd(),
  home: process.env.HOME ?? '',
})
process.exit(code)
```

- [ ] **Step 5: テストが通ることを確認**

Run: `cd apps/codex-bridge && bun test src/generate.test.ts`
Expected: PASS（15 tests）

- [ ] **Step 6: 冪等性を手動確認**

Run:
```bash
cd apps/codex-bridge && chmod +x bin/codex-bridge.ts
cd /tmp && rm -rf cbtest && mkdir -p cbtest/.claude/rules cbtest/go && cd cbtest
printf -- '---\ncodexTargets:\n  - go/\n---\n# Go rule\nbody\n' > .claude/rules/go-backend.md
printf '# overview\n' > CLAUDE.md
bun <ABS_PATH>/apps/codex-bridge/bin/codex-bridge.ts
cp go/AGENTS.md /tmp/first && bun <ABS_PATH>/apps/codex-bridge/bin/codex-bridge.ts && diff /tmp/first go/AGENTS.md
```
Expected: 2 回目でも `diff` が差分なし（冪等）。`go/AGENTS.md` と `AGENTS.md` が生成される。

- [ ] **Step 7: commit**

```bash
git add apps/codex-bridge/src/generate.ts apps/codex-bridge/src/generate.test.ts apps/codex-bridge/bin/codex-bridge.ts
git commit -m "feat(codex-bridge): 書き込み・main・CLI ラッパを追加"
```

---

### Task 8: スラッシュコマンドとプラグイン CLAUDE.md

`/codex-bridge` コマンド定義と、プラグイン固有の開発ガイドを追加する。

**Files:**
- Create: `apps/codex-bridge/commands/codex-bridge.md`
- Create: `apps/codex-bridge/CLAUDE.md`

- [ ] **Step 1: コマンド定義を作成**

`apps/codex-bridge/commands/codex-bridge.md`:

```markdown
---
allowed-tools: Bash
description: "Claude の rules/CLAUDE.md を Codex 用 AGENTS.md にネスト生成する"
---

# /codex-bridge

`.claude/rules/*.md` の frontmatter `codexTargets` に従って各ディレクトリの `AGENTS.md` を生成し、
root `CLAUDE.md` を root `AGENTS.md` に変換する。`--global` 指定時は `~/.claude/CLAUDE.md` を
`~/.codex/AGENTS.md` に変換する（Codex を日本語応答にするため）。

## 実行

引数なし（プロジェクト生成）:

```bash
bun "${CLAUDE_PLUGIN_ROOT}/bin/codex-bridge.ts"
```

`$ARGUMENTS` に `--global` が含まれる場合はグローバル生成:

```bash
bun "${CLAUDE_PLUGIN_ROOT}/bin/codex-bridge.ts" $ARGUMENTS
```

実行後、生成/スキップ/警告のサマリをユーザーに提示する。手書き（センチネル無し）の `AGENTS.md` は
上書きされずスキップされる点、32KB 超過ファイルは切り捨てリスクがある点を必要に応じて伝える。
```

- [ ] **Step 2: プラグイン CLAUDE.md を作成**

`apps/codex-bridge/CLAUDE.md`:

```markdown
# codex-bridge

Claude Code の rules / CLAUDE.md を Codex 用 `AGENTS.md` にネスト生成する汎用プラグイン。

## 仕組み

- `.claude/rules/*.md` の frontmatter `codexTargets: [<dir>/, ...]` を正として、各 `<dir>/AGENTS.md` を生成。
- root `CLAUDE.md` → root `AGENTS.md`。
- `--global`: `~/.claude/CLAUDE.md` → `~/.codex/AGENTS.md`。
- 生成物は先頭に `GENERATED by codex-bridge` センチネルを持つ。センチネル無しの既存 `AGENTS.md` は手書き扱いで上書きしない。
- Codex の `project_doc_max_bytes`（既定 32KB）超過は警告のみ。分割は `codexTargets` をより深いディレクトリに割り当てて対応する。

## 開発

```bash
bun test              # src/generate.test.ts
pnpm --filter @tanaka-yui/codex-bridge check
```

ロジックは `src/generate.ts` の純粋関数（parse / render / group / decide / size）と、fs を触る収集・書き込み・main に分離されている。純粋関数を優先して TDD する。

## 関連プラグインとの境界

cmux 系プラグイン（cmux-team 等）とは責務が独立。本プラグインは「Claude 設定 → Codex 設定」の一方向変換のみを担い、cmux トポロジや実行オーケストレーションには関与しない。
```

- [ ] **Step 3: 全体 check とテストを確認**

Run: `cd apps/codex-bridge && pnpm check && bun test`
Expected: PASS（型・lint・15 tests すべて）

- [ ] **Step 4: commit**

```bash
git add apps/codex-bridge/commands/codex-bridge.md apps/codex-bridge/CLAUDE.md
git commit -m "feat(codex-bridge): スラッシュコマンドとプラグインガイドを追加"
```

---

## 完了時の最終確認

- [ ] ルートで `pnpm check` が通る（turbo が codex-bridge に伝播）
- [ ] `apps/codex-bridge` で `bun test` が全パス
- [ ] `plugin.json` と `marketplace.json` の version が一致（0.1.0）
- [ ] 実在プロジェクト（influencer-platform）へ試験実行し、`go/AGENTS.md` 等が生成され Codex が rule を読むこと、`--global` で `~/.codex/AGENTS.md` が生成され Codex が日本語応答になることを確認
