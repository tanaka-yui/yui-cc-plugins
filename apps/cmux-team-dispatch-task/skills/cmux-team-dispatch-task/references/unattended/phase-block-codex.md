Phase A: use the Codex design pane to produce and commit a plan. Phase B: always delegate execution to the selected opus[1m], sonnet, or codex pane; the design pane must defer status ownership. Mark the selected status owner before sending execution instructions. Deliver those instructions with ONE call to `bash <skill directory>/scripts/send-prompt.sh --to-surface <target pane surface> --label phase-b-exec --outbox-dir <status directory>/outbox -- <request text>`, never by typing into a pane yourself. Do not request interactive input; record unresolved decisions in the result and end the session after completion.
