Review is enabled. Use the Review runner and Review engine printed in the header; do not derive either from the design or execution engine. After committing changes, request review from the configured pane. The request must require `code-round-N.md` in the status review directory and its last line must be `VERDICT: approve` or `VERDICT: needs_work`. The request must also tell the reviewer to send you ONE message whose body starts with `review-verdict:` right after it writes that file. Do not poll the file: arm ONE single-shot safety timer, stop, and wait for that message, and re-read the file on every wake because only the message can be lost. On needs_work, apply valid findings, commit, and repeat for at most five rounds. If the fifth round still needs work, record unresolved findings and continue. Do not ask a human question.
