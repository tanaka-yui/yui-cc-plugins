Review is enabled. After committing changes, request review from the configured pane. The request must require `code-round-N.md` in the status review directory and its last line must be `VERDICT: approve` or `VERDICT: needs_work`. Poll every 5 seconds. On needs_work, apply valid findings, commit, and repeat for at most five rounds. If the fifth round still needs work, record unresolved findings and continue. Do not ask a human question.
