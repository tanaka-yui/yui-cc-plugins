Phase B-R is assigned to the `exec_review` pane. Request each review with ONE call to agmsg `send.sh`: `~/.agents/skills/agmsg/scripts/send.sh <team> <your agent name> <Exec review agent> 'review-code: <request text>'`. The recipient is the Exec review agent printed in the Phase B-R block, never a surface or workspace id, and a non-zero exit means the reviewer was NOT told, so report it instead of waiting for a verdict that can never appear. Read the reviewer engine from the same exec-review tuple; never derive it from another role. Resolve the reviewer pane surface from the `exec_review` entry of prewarm.json in the status directory for the liveness check only. Never deliver a message by typing it into a pane yourself. The request must tell the reviewer to send you ONE message whose body starts with `review-verdict:` right after it writes the findings file. Do not poll that file, and do not watch the reviewer pane.

**As a claude session, arm ONE single-shot safety timer** — one `sleep $((30 * 60))`, a single sleep and never a loop — with the Bash tool and `run_in_background: true`, then stop and wait. **As a codex session you have NO safety net for this wait and you cannot build one**: a backgrounded subshell that sleeps and then messages you, and the detached `nohup` form of the same, were both measured on 2026-08-21 (D-T2) and both die when the codex turn ends. Do not write one. As a codex session, before stopping: (a) confirm the reviewer is reachable now — run `bash <skill dir>/scripts/verify-agmsg-ready.sh --codex --team <team> --name <Exec review agent>` when the Exec review engine is codex, otherwise check its pane once for liveness — and if it is not reachable, record that and proceed; (b) send ONE message with `~/.agents/skills/agmsg/scripts/send.sh <team> <your agent name> parent 'dispatch-notify: <your agent name> waiting for code review verdict; this engine has no timer'`, so the parent's timer remains the backstop. Re-read the findings file on every wake. A claude session stops its timer as soon as it acts on a verdict (`TaskStop`). If the timer fires with no VERDICT line, check the reviewer pane once, re-send the same round once, and re-arm the same timer, up to 3 times for that round. If the 3rd re-arm also fires with no verdict, record a skipped review and proceed.

If you stop before completing the work, do not stop silently. Write the reason to the round findings file with a last line of `VERDICT: needs_work`, tell the exec-review pane with one more `send.sh` call whose body starts with `abort-reviewer: [abort]` followed by a one-line reason, write status `error` with the reason as message, then notify the `parent` agent with one final `send.sh` call whose body starts with `dispatch-notify:`, and end the session. Writing the status file alone notifies no one.
