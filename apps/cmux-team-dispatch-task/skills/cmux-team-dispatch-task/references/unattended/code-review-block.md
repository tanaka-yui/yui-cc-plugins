Request each review with ONE call to agmsg `send.sh`: `~/.agents/skills/agmsg/scripts/send.sh <team> <your agent name> <Review pane agent> 'review-code: <request text>'`. The recipient is the Review pane agent printed in the header, never a surface or workspace id, and a non-zero exit means the reviewer was NOT told, so report it instead of waiting for a verdict that can never appear. Read `reviewer_engine` from the review configuration; it must be the Review engine printed in the header, and you must never derive it from another role. Resolve the reviewer pane surface from the `review` entry of the prewarm.json in the status directory for the liveness check only. Never deliver a message by typing it into a pane yourself. The request must tell the reviewer to send you ONE message whose body starts with `review-verdict:` right after it writes the findings file. Do not poll that file, and do not watch the reviewer pane. **As a claude session, arm ONE single-shot safety timer** — one `sleep $((30 * 60))`, a single sleep and never a loop — with the Bash tool and `run_in_background: true`, then stop and wait. **As a codex session you have NO safety net for this wait and you cannot build one**: a backgrounded subshell that sleeps and then messages you, and the detached `nohup` form of the same, were both measured on 2026-08-21 (D-T2) and both die when the codex turn ends, so a timer you believe you armed never fires. Do not write one — a timer that looks armed but is not is worse than none, because you would stop believing you are covered. As a codex session do these two things instead, before you stop: (a) confirm the reviewer is reachable now — run `bash <skill dir>/scripts/verify-agmsg-ready.sh --codex --team <team> --name <Review pane agent>` when the Review engine in the header is codex, otherwise check its pane once for liveness — and if it is not reachable, record that and proceed instead of waiting for a verdict that can never appear; (b) send ONE message with `~/.agents/skills/agmsg/scripts/send.sh <team> <your agent name> parent 'dispatch-notify: <your agent name> waiting for code review verdict; this engine has no timer'`, so the parent (whose own 90-minute timer is the only backstop left) can see a waiter that never came back. Re-read the findings file on every wake, whether a message or the timer woke you, because only the message can be lost; a claude session stops its timer as soon as it acts on a verdict (`TaskStop`), a codex session has nothing to stop. If the timer fires with no VERDICT line (claude only — a codex session never gets this wake), check the reviewer pane once for liveness, re-send the same round once, and re-arm the same timer; keep re-arming up to 3 times for that round. If the 3rd re-arm also fires with no verdict, record a skipped review and proceed — this is an unattended run, so there is nobody to ask.

If you stop before completing the work, do not stop silently. Write the reason to the round findings file with a last line of `VERDICT: needs_work`, tell the reviewer with one more `send.sh` call that is identical to the review request except that the body starts with `abort-reviewer: [abort]` followed by a one-line reason, write status `error` with the reason as message, then notify the parent with one final `send.sh` call addressed to the `parent` agent whose body starts with `dispatch-notify:`, and end the session. Writing the status file alone notifies no one.
