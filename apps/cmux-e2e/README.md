# cmux-e2e

cmux 内蔵ブラウザで、認証を含む UI フローを画面で確認しながら E2E テストするプラグイン。

`up` でサーフェスを確保し、`auth save` で state を保存してから、`.cmux-e2e-scenarios/` のシナリオを `run` で実行する。成果物は `.cmux-e2e-results/` に保存される。

## ライセンス

MIT
