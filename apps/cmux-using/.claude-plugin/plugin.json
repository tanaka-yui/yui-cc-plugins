{
  "name": "cmux-using",
  "version": "1.1.0",
  "description": "Skill for operating cmux terminal multiplexer. Pane splitting, sub-agent spawning/monitoring/result collection, command sending, screen reading, and notifications.",
  "author": {
    "name": "tanaka-yui"
  },
  "license": "MIT",
  "repository": "https://github.com/tanaka-yui/yui-cc-plugins/apps/cmux-using",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"$CMUX_SURFACE_ID\" ]; then REF=$(cmux identify | jq -r '.caller.surface_ref'); NUM=$(echo \"$REF\" | cut -d: -f2); cmux rename-tab --surface \"$REF\" \"[$NUM] Claude Code\"; fi"
          }
        ]
      }
    ]
  }
}